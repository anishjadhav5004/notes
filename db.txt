trucate cant be rolled backed bt delet from can

cursor is used for  line to line changes in db bcoz usually changes happens in db in group or in a set
🗑️ DELETE → Removes selected rows, slower, safer, reversible.
💣 TRUNCATE → Removes all rows instantly, faster, resets identity, not reversible.

✅ Yes, in some databases (like SQL Server, PostgreSQL, and Oracle),
you can use TRUNCATE inside a transaction, and you can roll it back if you don’t commit yet.

❌ But in others (like MySQL with MyISAM engine),
TRUNCATE auto-commits — it can’t be rolled back even if you’re inside a transaction.

upsert in tsql

merge into table1 as Target
  using( VALUES (1, 'Anish') ) as source
  on Target.id = source.id
when matched then
update Target
 set Target.name = source.name

when not matched then
  insert(name)
  values(source.name)
  
